# T1 format consistency QA: 2026-07-17

## Result

The current T1 CSV, Markdown and LaTeX representations are structurally and
cell-wise consistent. This format PASS is separate from the previously recorded
evidence-coverage gap and does not make any paper formally reproduced.

## File hashes

| File | SHA256 |
|---|---|
| `tables/t1.csv` | `d0f9c6968ed80ab7a3f6c5c79ae063ab064bd7208192aebe09f5aab6870fba65` |
| `tables/t1.md` | `b1eccac8a076fdaf06efeb91930c4bf6e9ae371739b56174cff7a790ed898dc5` |
| `tables/t1.tex` | `c0059dbeb31491cf12cded9c0518fe2eec9c926ab55748c3f36a252aa02b9006` |

## Independent consistency checks

- UTF-8 decoding succeeds for all three files.
- CSV contains 13 columns and six data rows.
- The rows comprise five `main` papers and one `supplementary` paper.
- Markdown headers and all data cells equal the CSV after removing Markdown
  delimiters only.
- LaTeX headers and all data cells equal the CSV after reversing LaTeX escaping
  only; empty terminal cells are preserved.
- All six `pin_match` cells are `True`.
- All six `source_audit_pass` cells are `True`.
- All six `formal_eligible` cells are `False`; the formal-eligible count is zero.

The exact independent check status is
`PASS_FORMAT_CONSISTENCY_WITH_COVERAGE_GAP`.

## Residual coverage gap

The table does not yet expose source-smoke status, environment probe status,
environment lock SHA, execution role or local unified-protocol status. HARP and
ESDA therefore have `formal_eligible=False` with an empty displayed blocker. The
required joined-schema repair is specified in
`t1_reproducibility_coverage_gap_20260717.md` and must be implemented only after
the active G1 source-frozen run closes and a new freeze is created.

## Claim boundary

T1 may currently be cited as a consistent source/version/license audit table.
It may not be cited as proof that any author result was reproduced, that a local
unified-protocol comparison is complete, or that HARP/ESDA are formally eligible.
The three files must continue to be regenerated together; no representation may
be hand-edited independently.
