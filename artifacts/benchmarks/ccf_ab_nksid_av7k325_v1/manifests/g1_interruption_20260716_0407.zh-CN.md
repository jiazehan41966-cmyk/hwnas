# G1 中断证据（中文伴随档案，2026-07-16 04:07 CST）

- 英文原件：`g1_interruption_20260716_0407.md`；SHA256：`5ec0208ac87cea35eef1b79a4f58825fa966103c8cfc5c4a5bdc4a11922dd0ea`。
- 分类：`external_system_shutdown`；launcher return code `1073807364`（`0x40010004`）。
- Windows System log 在 04:07:07–04:07:31 记录 `winsrvext` shutdown-delay event；launcher log 没有 Python traceback、CUDA OOM 或 protocol exception。
- 当时 scratch 完成 15/15；但其 `code_patch.diff` 后来被失败 resume 覆盖，因此后续独立审计政策只允许把该运行用于诊断，不能替代 scratch-v2 正式 cohort。
- 预训练保留 4 个原子单元至 fold1/seed42；fold1/seed43 在最后 flush 的 epoch108 后停止，没有原子记录。
- 07:28 resume 因 run fingerprint 变化在训练前被拒绝，未使用 `--force`。
- 诊断：`git_code_provenance` 包含可变 benchmark/gate JSON，即使 556-file source freeze 仍完全通过，也会使 fingerprint 变化；拒绝的 resume 还在兼容性检查前重写 scratch `code_patch.diff`，但 immutable source snapshot 未受影响。
- 恢复策略：把 4 个预训练原子记录归档为中断证据，在预期正式目录用单一新 fingerprint 重跑全部 15 单元；不混合新旧 fingerprint，不保留 partial epoch，不覆盖完成记录。

本中断记录不支持模型性能结论。
