# G1 interruption evidence: 2026-07-16 04:07 CST

- Classification: `external_system_shutdown`.
- Launcher return code: `1073807364` (`0x40010004`).
- Windows System log records `winsrvext` shutdown-delay events between 04:07:07 and 04:07:31.
- The launcher log contains no Python traceback, CUDA OOM, or protocol exception.
- `scratch_mobilenet_v2` completed 15/15 pairs and is protocol-claimable.
- `imagenet_pretrained_mobilenet_v2` retained four atomic pairs through fold 1 / seed 42. Fold 1 / seed 43 stopped after the last flushed epoch 108 and produced no atomic record.
- A 07:28 resume attempt was rejected before training because the run fingerprint had changed. No `--force` override was used.
- Diagnosis: `git_code_provenance` incorporates mutable benchmark/gate JSON artifacts even though the 556-file source-freeze snapshot still verifies exactly. The rejected attempt also rewrote the scratch `code_patch.diff` before checking compatibility; the original patch SHA remains in the scratch manifest and the immutable source snapshot is unaffected.
- Recovery therefore archives the four pretrained atomic records as interruption evidence and restarts all 15 pretrained fold-seed units under one new fingerprint in the expected formal result directory. Old and new fingerprints are not mixed, partial epoch state is discarded, and no completed record is overwritten.

This interruption record does not support any model-performance claim.
